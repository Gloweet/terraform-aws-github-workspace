name: Setup AWS OIDC
description: Configures AWS credentials and verifies identity.
# Note: This action expects aws-role-arn and aws-region to be passed from environment variables/secrets
inputs:
  aws-role-arn:
    description: The ARN of the AWS role to assume (use environment secrets)
    required: true
  aws-region:
    description: The AWS region to use (use environment variables)
    required: true

runs:
  using: "composite"
  steps:
    - name: Configure AWS credentials via OIDC
      uses: aws-actions/configure-aws-credentials@v2
      with:
        role-to-assume: $${{ inputs.aws-role-arn }}
        aws-region: $${{ inputs.aws-region }}

    - name: Verify IAM Role Assumption
      run: aws sts get-caller-identity
      shell: bash
